---
"@onda-engine/components": minor
---

Add **CardShowcase** — a tilted card-conveyor brand showcase component — and **ImageReveal** `motion: 'none'` (held still, no entrance). Both are registered in the manifest as agent-facing.
