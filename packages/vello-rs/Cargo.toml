[package]
name = "onda-vello"
description = "Vello-based vector renderer for ONDA (paths, strokes, gradients, AA). Step 1 — probe."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

# onda-core / onda-scene join here in step 2 (mapping the scene graph onto Vello).
[dependencies]
pollster = "0.4"
vello = "0.3.0"

[dev-dependencies]
png = "0.18"
