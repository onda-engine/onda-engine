[package]
name = "onda-gpu"
description = "GPU backend for the ONDA renderer (wgpu): offscreen render of the scene graph to an RGBA framebuffer. Step 1 — pipeline + shapes."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[dependencies]
onda-core = { workspace = true }
onda-scene = { workspace = true }
onda-typography = { workspace = true }
bytemuck = { version = "1", features = ["derive"] }
pollster = "0.4"
wgpu = "24.0.5"

[dev-dependencies]
png = "0.18"
