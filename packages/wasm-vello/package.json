{
  "name": "@onda-engine/wasm-vello",
  "version": "0.1.0",
  "description": "The ONDA Vello (GPU) renderer compiled to WebAssembly — Vello in the browser over WebGPU (no DOM, no Chromium).",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "main": "./index.js",
  "types": "./index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "pkg"
  ],
  "scripts": {
    "build": "cargo build -p onda-wasm-vello --target wasm32-unknown-unknown --release && wasm-bindgen ../../target/wasm32-unknown-unknown/release/onda_wasm_vello.wasm --out-dir pkg --target web"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/onda-engine/onda-engine.git",
    "directory": "packages/wasm-vello"
  }
}
