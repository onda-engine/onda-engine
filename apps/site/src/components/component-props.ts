// Generated prop reference — extracted from each component's exported
// `<Name>Props` interface (name / type / required / description). Regenerated
// from the component sources; avoid hand-edits.
export interface PropSpec {
  name: string
  type: string
  required: boolean
  description: string
}

export const COMPONENT_PROPS: Record<string, PropSpec[]> = {
  KineticText: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The line to animate, one glyph at a time.',
    },
    { name: 'fontSize', required: false, type: 'number', description: 'Font size in px.' },
    {
      name: 'preset',
      required: false,
      type: "'rise' | 'fade' | 'scale' | 'blur' | 'wave'",
      description:
        'Per-glyph entrance style. `blur` is a real per-glyph render-to-texture focus pull.',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between each glyph (default the house STAGGER).',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Per-glyph entrance duration in frames.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the first glyph begins.',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: 'Horizontal alignment of the line. Default `center`.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme text).',
    },
    { name: 'fontFamily', required: false, type: 'string', description: 'Loaded font family.' },
    { name: 'fontWeight', required: false, type: 'number', description: 'CSS weight 1..1000.' },
  ],
  FilmGrade: [
    {
      name: 'look',
      required: false,
      type: "'warm' | 'cool' | 'noir' | 'teal-orange' | 'vibrant' | 'film' | 'faded'",
      description: 'The named cinematic look applied to the whole subtree. Default `film`.',
    },
    {
      name: 'intensity',
      required: false,
      type: 'number',
      description: 'Strength of the look, `0..1` (lerps every param from neutral). Default `1`.',
    },
    {
      name: 'exposure',
      required: false,
      type: 'number',
      description: 'Explicit linear-exposure override (`2^exposure`), applied on top of the look.',
    },
    {
      name: 'contrast',
      required: false,
      type: 'number',
      description: 'Explicit contrast override (1 = identity), on top of the look.',
    },
    {
      name: 'saturation',
      required: false,
      type: 'number',
      description: 'Explicit saturation override (1 = identity, 0 = grayscale), on top.',
    },
    {
      name: 'temperature',
      required: false,
      type: 'number',
      description: 'Explicit warm/cool override (positive = warm), on top of the look.',
    },
    {
      name: 'tint',
      required: false,
      type: 'number',
      description: 'Explicit green/magenta override (positive = green), on top.',
    },
  ],
  AudioClip: [
    {
      name: 'src',
      required: false,
      type: 'string',
      description: 'URL or path to the audio file. AAC-in-MP4 or WAV preferred.',
    },
    {
      name: 'startAt',
      required: false,
      type: 'TimeSpec',
      description:
        'Where to start in the source audio. Time spec — `"0:04"`, `"30s"`, `"500ms"`, `"90f"`, or a raw number of seconds.',
    },
    {
      name: 'endAt',
      required: false,
      type: 'TimeSpec',
      description:
        "Where to stop in the source. Same time spec as `startAt`. When omitted, plays to the source's end. Required for `loop`.",
    },
    {
      name: 'volume',
      required: false,
      type: 'number',
      description: 'Amplitude volume `0..1`.',
    },
    {
      name: 'gainDb',
      required: false,
      type: 'number',
      description:
        'Advanced gain in dB. When set, wins over `volume`. Converted via `10 ** (dB / 20)`: `0` = unity, `-6` ≈ 0.5, `-12` ≈ 0.25, `-20` ≈ 0.1.',
    },
    {
      name: 'fade',
      required: false,
      type: 'boolean',
      description:
        'Apply an entry/exit volume envelope. Default `true` with a tiny 2-frame click-guard fade. Set larger `fadeDuration` for audible bed fades.',
    },
    {
      name: 'fadeDuration',
      required: false,
      type: 'number',
      description: 'Frames the fade-in / fade-out takes. Default 2 (~67ms @ 30fps).',
    },
    {
      name: 'loop',
      required: false,
      type: 'boolean',
      description: 'Loop the trimmed clip. Requires `endAt` (loop interval is `endAt - startAt`).',
    },
    {
      name: 'muted',
      required: false,
      type: 'boolean',
      description: 'Mute the clip.',
    },
    {
      name: 'playbackRate',
      required: false,
      type: 'number',
      description: 'Playback speed (browser-clamped 0.0625..16 in ondajs).',
    },
    {
      name: 'acceptableTimeShiftSeconds',
      required: false,
      type: 'number',
      description: 'Acceptable time-shift threshold before resync (seconds).',
    },
  ],
  AudioVisualizer: [
    {
      name: 'src',
      required: false,
      type: 'string',
      description:
        'Audio file URL to drive the bars with REAL frequency data (decoded + FFT’d by @onda/wasm-audio — identical spectra in preview and export). Omit for the built-in procedural animation. Browser preview needs same-origin/CORS; `onda export` accepts any direct URL.',
    },
    {
      name: 'type',
      required: false,
      type: "'bars' | 'mirrored' | 'waveform' | 'radial' | 'dots'",
      description:
        'Render style (default `bars`). All styles draw from the same amplitude data — use the toggle above the preview to compare. `waveform` (path) and `radial` (rotation) are GPU-only.',
    },
    {
      name: 'barCount',
      required: false,
      type: 'number',
      description: 'Number of frequency bands.',
    },
    {
      name: 'color',
      required: false,
      type: 'string | string[]',
      description:
        'Bar color. Pass a single hex string for a one-tone visualizer, or a two-entry array `[top, bottom]` for a vertical gradient ramp. The FIRST entry is the meaningful color on the CPU backend (see header). (default: theme `accent` for the top, theme `palette[1]` for the bottom)',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Overall width of the bar row, in px.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Overall height of the bar row (the tallest a bar can reach), in px.',
    },
    {
      name: 'align',
      required: false,
      type: "'top' | 'middle' | 'bottom'",
      description: 'Vertical placement of the bars within `height`.',
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Pixel gap between adjacent bars.',
    },
    {
      name: 'barRadius',
      required: false,
      type: 'number',
      description:
        'Bar corner radius in px (also the minimum bar height so idle bars read) (default: theme `radius`).',
    },
    {
      name: 'speed',
      required: false,
      type: 'number',
      description: "Animation speed multiplier for the fake spectrum's drift.",
    },
    {
      name: 'seed',
      required: false,
      type: 'number | string',
      description: 'Deterministic seed for the fake spectrum.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the visualizer fades/grows in.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames for the entrance grow-in.',
    },
  ],
  BarChart: [
    {
      name: 'data',
      required: false,
      type: 'BarChartDatum[]',
      description: 'Bars to render. Order is preserved — top to bottom.',
    },
    {
      name: 'max',
      required: false,
      type: 'number',
      description: 'Value mapped to a full-width bar. Bars cap at 100% of the track.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the **first** bar starts.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Per-bar grow duration. Bars want more time than text (default `slow`).',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between consecutive bars (default canonical `STAGGER` = 4).',
    },
    {
      name: 'barHeight',
      required: false,
      type: 'number',
      description: 'Bar (and track) height in px.',
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Pixel gap between rows.',
    },
    {
      name: 'labelWidth',
      required: false,
      type: 'number',
      description: 'Pixels reserved for the label column (left of the track).',
    },
    {
      name: 'labelGap',
      required: false,
      type: 'number',
      description: 'Gap between the label column and the track, in px.',
    },
    {
      name: 'trackWidth',
      required: false,
      type: 'number',
      description: 'Track length in px — the full-width target for a bar at `max`.',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Color of the **largest** bar — the earned accent (Onda rose).',
    },
    {
      name: 'barColor',
      required: false,
      type: 'string',
      description: 'Color of non-largest bars.',
    },
    {
      name: 'trackColor',
      required: false,
      type: 'string',
      description: 'Bar track (background) color.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Label color.',
    },
    {
      name: 'showValues',
      required: false,
      type: 'boolean',
      description: 'Show the numeric value at the end of each bar.',
    },
    {
      name: 'countUp',
      required: false,
      type: 'boolean',
      description:
        "Count each value up from 0 in sync with its bar's growth (lands exactly on the true value). Only applies when `showValues` is on. Default `true`.",
    },
    {
      name: 'title',
      required: false,
      type: 'string',
      description:
        'Optional headline above the chart — tells viewers what the numbers measure (e.g. "Frames per second").',
    },
    {
      name: 'titleSize',
      required: false,
      type: 'number',
      description: 'Title font size in px. Default ~1.5× the label `fontSize`.',
    },
    {
      name: 'titleColor',
      required: false,
      type: 'string',
      description: 'Title color. Defaults to `color`.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label / value font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family for labels and values.',
    },
  ],
  BentoGrid: [
    {
      name: 'items',
      required: false,
      type: 'BentoItem[]',
      description: 'The cells, laid out left-to-right, top-to-bottom. Spans drive the rhythm.',
    },
    {
      name: 'columns',
      required: false,
      type: 'number',
      description: 'Number of grid columns (default 3).',
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Gap between cells in px (default 20).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Overall grid width in px (default 960).',
    },
    {
      name: 'rowHeight',
      required: false,
      type: 'number',
      description: 'Row-track height in px. Defaults to the column-track width (≈ square cells).',
    },
    {
      name: 'padding',
      required: false,
      type: 'number',
      description: 'Inner padding of each cell in px (default 28).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the first cell enters (default 0).',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between successive cells rising in. House stagger is 4.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Base title font size in px (default 30).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Title color (default: theme `text`).',
    },
    {
      name: 'captionColor',
      required: false,
      type: 'string',
      description: 'Caption color (default: theme `textMuted`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Accent color for the earned `accent` cell (default: theme `accent`).',
    },
    {
      name: 'cardColor',
      required: false,
      type: 'string',
      description: 'Card fill — translucent dark, approximating glass (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Card border color (default: theme `border`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Display font family for titles and values (default: theme `headingFamily ?? fontFamily`).',
    },
    {
      name: 'captionFontFamily',
      required: false,
      type: 'string',
      description: 'Body font family for captions (default: theme `fontFamily`).',
    },
  ],
  BlurReveal: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description:
        'What to reveal. Rendered as a single-line `<Text>` unless `children` is provided.',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description:
        'Custom content to reveal instead of `text` (wins over `text` when both are given). Lets BlurReveal wrap any subtree, not just a string.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the reveal starts.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description:
        'Frames until the reveal fully settles (default `DURATION.base` = 18). With `SPRING_SMOOTH` the visible motion settles in roughly this range.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description:
        'Text color (hex `#rrggbb` / `#rrggbbaa`). Ignored when `children` is set. (default: theme `text`)',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Text size in px. Ignored when `children` is set.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family. Ignored when `children` is set. (default: theme `fontFamily`)',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600). Ignored when `children` is set.',
    },
    {
      name: 'placement',
      required: false,
      type: "'center' | 'top' | 'bottom'",
      description: 'Vertical placement within the composition.',
    },
    {
      name: 'travelPx',
      required: false,
      type: 'number',
      description: "Rise distance in px (the original's 16px envelope; small on purpose).",
    },
    {
      name: 'fromScale',
      required: false,
      type: 'number',
      description:
        'Starting scale for the focus-settle (the dropped-blur approximation). Close to 1 by design — below ~0.92 it reads as a zoom, not a focus-in.',
    },
  ],
  BoundingBox: [
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Box left edge as a `0..1` fraction of the composition width.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Box top edge as a `0..1` fraction of the composition height.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Box width as a `0..1` fraction of the composition width.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Box height as a `0..1` fraction of the composition height.',
    },
    {
      name: 'label',
      required: false,
      type: 'string',
      description: "Optional label tag pinned to the box's top-left corner. Empty hides it.",
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description:
        'Outline / tick / tag color — the earned Onda rose by default (default: theme `accent`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the outline starts revealing.',
    },
    {
      name: 'drawDuration',
      required: false,
      type: 'number',
      description: 'Frames to reveal the full outline (default `DURATION.slow` = 24).',
    },
    {
      name: 'strokeWidth',
      required: false,
      type: 'number',
      description: 'Outline stroke width in px.',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description:
        'Corner radius of the outline in px. Defaults to `0` (sharp corners, like the ondajs selection marquee); set > 0 to round the outline.',
    },
    {
      name: 'corners',
      required: false,
      type: 'boolean',
      description: 'Draw small L-shaped tick marks at each corner after the outline lands.',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Label text color — a dark for contrast on the accent tag by default.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Label font family (must be loaded by the renderer) (default: theme `headingFamily`).',
    },
  ],
  BrowserFrame: [
    {
      name: 'url',
      required: false,
      type: 'string',
      description: 'URL shown in the address pill (and as the placeholder when empty).',
    },
    {
      name: 'src',
      required: false,
      type: 'string',
      description:
        'Image to show inside the frame when no `children` are passed. Scaled to fill the content width (see approximations).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the entrance.',
    },
    {
      name: 'animate',
      required: false,
      type: 'boolean',
      description: 'Scale-and-fade the frame in on the house spring.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Frame (and content) width in px.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Content height in px (excludes the chrome bar).',
    },
    {
      name: 'from',
      required: false,
      type: 'number',
      description: 'Starting scale for the entrance (default 0.96 — restrained, like ondajs).',
    },
    {
      name: 'surface',
      required: false,
      type: 'string',
      description: 'Card body fill (default: theme `surface`).',
    },
    {
      name: 'border',
      required: false,
      type: 'string',
      description: '1px card border and chrome divider (default: theme `border`).',
    },
    {
      name: 'borderLit',
      required: false,
      type: 'string',
      description: 'Traffic-light dot color (default: theme `border`).',
    },
    {
      name: 'surface2',
      required: false,
      type: 'string',
      description: 'Address-pill fill (default: theme `surface`).',
    },
    {
      name: 'bg',
      required: false,
      type: 'string',
      description: 'Content canvas background (default: theme `background`).',
    },
    {
      name: 'dim',
      required: false,
      type: 'string',
      description: 'Pill/URL text color (default: theme `textMuted`).',
    },
    {
      name: 'faint',
      required: false,
      type: 'string',
      description: 'Placeholder text color (default: theme `textMuted`).',
    },
    {
      name: 'cardRadius',
      required: false,
      type: 'number',
      description: 'Card corner radius in px (default: theme `radius`).',
    },
    {
      name: 'pillRadius',
      required: false,
      type: 'number',
      description: 'Address-pill corner radius in px (default: theme `radius`).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: "Content to wrap; renders at the content area's local origin (0,0).",
    },
  ],
  Button: [
    {
      name: 'label',
      required: false,
      type: 'string',
      description: 'The button label.',
    },
    {
      name: 'variant',
      required: false,
      type: "'primary' | 'ghost'",
      description:
        "`'primary'` = filled with `color`; `'ghost'` = transparent with a `color` border and `color`-tinted label.",
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description:
        'Accent color — the primary fill and the ghost border/label tint (default: theme `accent`).',
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description:
        "Label color on the primary variant (default: theme `text`). Ignored by `'ghost'`, which tints the label with `color`.",
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Pill width in px (default 280).',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Pill height in px (default 72).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description: 'Corner radius in px (default: theme `radius`).',
    },
    {
      name: 'borderWidth',
      required: false,
      type: 'number',
      description: "Border thickness in px for the `'ghost'` variant (default 2).",
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px (default 24).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Label font weight (display default 600).',
    },
    {
      name: 'centerX',
      required: false,
      type: 'number',
      description:
        'Horizontal center as a 0–1 fraction of canvas width (default 0.5 — centered, mirroring the ondajs `placement` default).',
    },
    {
      name: 'centerY',
      required: false,
      type: 'number',
      description: 'Vertical center as a 0–1 fraction of canvas height (default 0.5).',
    },
    {
      name: 'entrance',
      required: false,
      type: 'boolean',
      description: 'Play the entrance (fade + rise on the house spring).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the entrance begins.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Entrance duration in frames (default `DURATION.base` = 18).',
    },
    {
      name: 'press',
      required: false,
      type: 'boolean',
      description: 'Play the click-dip press animation.',
    },
    {
      name: 'pressFrame',
      required: false,
      type: 'number',
      description: 'Frame the press dip lands on (relative to the local timeline).',
    },
  ],
  Callout: [
    {
      name: 'label',
      required: false,
      type: 'string',
      description: 'Bubble label. Single line — no auto-wrap.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Bubble-center X as a 0..1 fraction of canvas width (default 0.5 = center).',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Bubble-center Y as a 0..1 fraction of canvas height (default 0.5 = center).',
    },
    {
      name: 'direction',
      required: false,
      type: 'CalloutDirection',
      description: "Side the pointer triangle sticks out from (default `'bottom'`).",
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the bubble starts revealing.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Bubble scale-and-fade reveal duration in frames (default `DURATION.base`).',
    },
    {
      name: 'lineDelay',
      required: false,
      type: 'number',
      description: 'Frames after the bubble starts before the pointer eases in (default 6).',
    },
    {
      name: 'lineDuration',
      required: false,
      type: 'number',
      description: 'Pointer reveal duration in frames (default `DURATION.base`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Label color (default: theme `text`).',
    },
    {
      name: 'bgColor',
      required: false,
      type: 'string',
      description: 'Bubble background fill (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Bubble border color (default: theme `border`).',
    },
    {
      name: 'borderWidth',
      required: false,
      type: 'number',
      description: 'Bubble border width in px (default 1).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px (default 20).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family (the Onda display font) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Label font weight (default 500).',
    },
    {
      name: 'paddingX',
      required: false,
      type: 'number',
      description: 'Horizontal padding inside the bubble (default 14).',
    },
    {
      name: 'paddingY',
      required: false,
      type: 'number',
      description: 'Vertical padding inside the bubble (default 8).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description: 'Bubble corner radius (default: theme `radius`).',
    },
    {
      name: 'pointerWidth',
      required: false,
      type: 'number',
      description: 'Pointer triangle base width in px (default 18).',
    },
    {
      name: 'pointerLength',
      required: false,
      type: 'number',
      description: 'Pointer triangle length (how far it pokes out) in px (default 12).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Explicit bubble width in px. Overrides the glyph-count estimate.',
    },
  ],
  CameraShake: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the shake starts. Outside the window, offset is 0.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description:
        'Frames the shake lasts. Before `delay` / after `delay + duration`, the offset is exactly 0 and content is still.',
    },
    {
      name: 'intensity',
      required: false,
      type: 'number',
      description:
        'Maximum positional offset in px. Restrained by default — bump for impact moments.',
    },
    {
      name: 'rotationIntensity',
      required: false,
      type: 'number',
      description:
        'Maximum rotation amplitude in degrees (GPU/Vello only). A subtle wobble by default; set `0` for pure translational shake.',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description: 'PRNG seed — same seed always produces the same shake.',
    },
    {
      name: 'decay',
      required: false,
      type: 'boolean',
      description:
        'Linearly decay intensity (and rotation) to 0 over `duration`, so the camera settles to rest by the end. Default `true`.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Rest x of the wrapper in px (the shake jitters around this).',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Rest y of the wrapper in px (the shake jitters around this).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: 'Content to shake.',
    },
  ],
  Captions: [
    {
      name: 'captions',
      required: false,
      type: 'CaptionEntry[]',
      description:
        'The transcript timeline. Each entry is a word + its `[startMs, endMs)` window — the format every STT / transcript tool already speaks.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the timeline starts (shifts every `startMs` by this).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Inactive word color (the Onda dim) (default: theme `textMuted`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description:
        'Active word color (the Onda text color). The contrast moment is brightness, not a rose accent — captions appear in batches and a rose pulse on every word would burn the eye (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'letterSpacing',
      required: false,
      type: 'string',
      description:
        "CSS letter-spacing (e.g. `'-0.02em'`). Accepted for prop-shape parity with ondajs; the scene `<Text>` has no letter-spacing knob, so it is NOT applied (see `approximations`).",
    },
    {
      name: 'lineHeight',
      required: false,
      type: 'number',
      description:
        'Unitless line height. Accepted for prop-shape parity with ondajs; the scene `<Text>` has a fixed text box, so it is NOT applied (see `approximations`).',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: 'Text alignment of the caption block within its line(s).',
    },
    {
      name: 'placement',
      required: false,
      type: "'center' | 'top' | 'bottom' | 'upper-third' | 'lower-third'",
      description:
        "Vertical placement band of the block. Captions sit in the lower third by default; `'center'`/`'top'`/`'upper-third'`/`'bottom'` reposition it.",
    },
    {
      name: 'maxWidth',
      required: false,
      type: 'number',
      description:
        'Max line width as a 0–1 fraction of canvas width — the block wraps within this (default 0.8).',
    },
  ],
  ChapterCard: [
    {
      name: 'chapter',
      required: true,
      type: 'string',
      description: 'The chapter heading — the focal text on the card.',
    },
    {
      name: 'number',
      required: false,
      type: 'string',
      description:
        'Numbered index above the chapter. String so leading zeros (`"01"`) read as intended.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the number starts fading in. The whole card sequences off this.',
    },
    {
      name: 'accent',
      required: false,
      type: 'boolean',
      description:
        'When `true`, the number takes `numberColor` (the rose) and an underline punctuates the title.',
    },
    {
      name: 'numberColor',
      required: false,
      type: 'string',
      description:
        'Number color when `accent` is `true` (the Onda rose) (default: theme `accent`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Chapter title color (default: theme `text`).',
    },
    {
      name: 'subtitleColor',
      required: false,
      type: 'string',
      description:
        'Number color when `accent` is `false` — quiet metadata dim (default: theme `textMuted`).',
    },
    {
      name: 'numberFontSize',
      required: false,
      type: 'number',
      description: 'Number font size in px — smaller than the title, sitting above it.',
    },
    {
      name: 'numberFontWeight',
      required: false,
      type: 'number',
      description: 'Number font weight.',
    },
    {
      name: 'titleFontSize',
      required: false,
      type: 'number',
      description: 'Chapter title font size in px — the focal element.',
    },
    {
      name: 'titleFontWeight',
      required: false,
      type: 'number',
      description: 'Title font weight.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Onda display font, applied to both number and title for tonal consistency (default: theme `headingFamily ?? fontFamily`).',
    },
  ],
  CodeBlock: [
    {
      name: 'code',
      required: false,
      type: 'string',
      description: 'The source to render. Newlines split into reveal-able lines.',
    },
    {
      name: 'title',
      required: false,
      type: 'string',
      description:
        'Filename shown in the title bar. Empty hides the title (dots still show if `chrome`).',
    },
    {
      name: 'chrome',
      required: false,
      type: 'boolean',
      description: 'Show the macOS-style window chrome (three dots + title bar).',
    },
    {
      name: 'revealLines',
      required: false,
      type: 'boolean',
      description: 'Reveal lines one-by-one instead of all at once.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the first line appears.',
    },
    {
      name: 'lineDelay',
      required: false,
      type: 'number',
      description: 'Frames between successive line reveals.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Monospace font stack. Real monospace by default — code needs column alignment (default: theme `monoFamily`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Code font size in px. Sized for a video canvas, not a screen UI.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Panel width in px.',
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description:
        'Default text color — identifiers, punctuation, operators (default: theme `text`).',
    },
    {
      name: 'keywordColor',
      required: false,
      type: 'string',
      description:
        'Keyword color. A muted, dusty violet — reads as syntax, not the brand accent (default: theme `palette[1]`).',
    },
    {
      name: 'stringColor',
      required: false,
      type: 'string',
      description: 'String literal color — dusty sage (default: theme `palette[3]`).',
    },
    {
      name: 'commentColor',
      required: false,
      type: 'string',
      description: 'Comment color (default: theme `textMuted`).',
    },
    {
      name: 'numberColor',
      required: false,
      type: 'string',
      description: 'Numeric literal color — dusty amber (default: theme `palette[2]`).',
    },
    {
      name: 'tagColor',
      required: false,
      type: 'string',
      description: 'JSX / HTML tag-name color — dusty cyan (default: theme `palette[1]`).',
    },
    {
      name: 'panelColor',
      required: false,
      type: 'string',
      description: 'Panel background fill (the "glass" surface) (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Panel border color (default: theme `border`).',
    },
  ],
  CodeDiff: [
    {
      name: 'lines',
      required: false,
      type: 'DiffLine[]',
      description: 'The diff lines, top to bottom.',
    },
    {
      name: 'title',
      required: false,
      type: 'string',
      description: 'Filename shown in the title bar.',
    },
    {
      name: 'chrome',
      required: false,
      type: 'boolean',
      description: 'Show window chrome (dots + title bar).',
    },
    {
      name: 'revealLines',
      required: false,
      type: 'boolean',
      description: 'Reveal lines one-by-one (else all appear together).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the first line appears.',
    },
    {
      name: 'lineDelay',
      required: false,
      type: 'number',
      description: 'Frames between consecutive line reveals (default canonical `STAGGER` = 4).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Monospace font stack for code (and the title) (default: theme `monoFamily ?? fontFamily`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Code font size in px.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Panel width in px.',
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description: 'Default (context) text color (default: theme `textMuted`).',
    },
    {
      name: 'addColor',
      required: false,
      type: 'string',
      description: 'Added-line color (default: theme `palette[3]`).',
    },
    {
      name: 'removeColor',
      required: false,
      type: 'string',
      description: 'Removed-line color (default: theme `accent`).',
    },
    {
      name: 'surfaceColor',
      required: false,
      type: 'string',
      description: 'Panel surface (glass) fill (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Panel border / chrome divider color (default: theme `border`).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description: 'Panel corner radius in px (default: theme `radius`).',
    },
    {
      name: 'chromeDotsColor',
      required: false,
      type: 'string',
      description: 'Window-chrome traffic-light dot color (default: theme `border`).',
    },
    {
      name: 'chromeTitleColor',
      required: false,
      type: 'string',
      description: 'Window-chrome title (filename) color (default: theme `textMuted`).',
    },
  ],
  Confetti: [
    {
      name: 'seed',
      required: false,
      type: 'number',
      description:
        'Seed for every per-piece random (angle, velocity, spin, colour, size) — the same seed always produces the same burst (§1).',
    },
    {
      name: 'count',
      required: false,
      type: 'number',
      description: 'Number of confetti pieces. ~80 reads full without thrashing the render.',
    },
    {
      name: 'colors',
      required: false,
      type: 'string[]',
      description:
        'Palette pieces are picked from. Defaults to the Onda accent plus tasteful neutrals (default: theme `accent`, theme `text`, theme `textMuted`, theme `border`).',
    },
    {
      name: 'originX',
      required: false,
      type: 'number',
      description: 'Burst origin X, as a fraction of canvas width (0 = left, 1 = right).',
    },
    {
      name: 'originY',
      required: false,
      type: 'number',
      description: 'Burst origin Y, as a fraction of canvas height (0 = top, 1 = bottom).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the burst launches.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames over which a piece travels, tumbles and fades out.',
    },
    {
      name: 'spread',
      required: false,
      type: 'number',
      description: 'Launch spread, in degrees, around straight up. Wider = more fan-out.',
    },
    {
      name: 'gravity',
      required: false,
      type: 'number',
      description: 'Downward acceleration. Higher = pieces fall back faster.',
    },
    {
      name: 'pieceSize',
      required: false,
      type: 'number',
      description: 'Base piece size in pixels — each piece varies around this.',
    },
  ],
  CountUp: [
    {
      name: 'from',
      required: false,
      type: 'number',
      description: 'Starting value (default `0`).',
    },
    {
      name: 'to',
      required: false,
      type: 'number',
      description: 'Ending value (default `100`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the count starts (default `0`).',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description:
        'Frames to count from `from` to `to`. Numbers want more time than text (default `DURATION.slow` = 24).',
    },
    {
      name: 'decimals',
      required: false,
      type: 'number',
      description: 'Fraction digits to render (default `0`).',
    },
    {
      name: 'useGrouping',
      required: false,
      type: 'boolean',
      description: 'Insert en-US thousands separators (default `true`).',
    },
    {
      name: 'prefix',
      required: false,
      type: 'string',
      description: "Prepended to the number, e.g. `'$'` (default `''`).",
    },
    {
      name: 'suffix',
      required: false,
      type: 'string',
      description: "Appended to the number, e.g. `'%'` (default `''`).",
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px. Counters are usually large (default `120`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (default `600`).',
    },
    {
      name: 'snappy',
      required: false,
      type: 'boolean',
      description:
        'Use the snappier spring (`SPRING_SNAPPY`) for the count (default `false`, i.e. `SPRING_SMOOTH` — matches ondajs).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Pixel translate for placement.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: '',
    },
  ],
  Cursor: [
    {
      name: 'fromX',
      required: false,
      type: 'number',
      description: 'Start X as a 0..1 fraction of canvas width.',
    },
    {
      name: 'fromY',
      required: false,
      type: 'number',
      description: 'Start Y as a 0..1 fraction of canvas height.',
    },
    {
      name: 'toX',
      required: false,
      type: 'number',
      description: 'End X as a 0..1 fraction of canvas width.',
    },
    {
      name: 'toY',
      required: false,
      type: 'number',
      description: 'End Y as a 0..1 fraction of canvas height.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the cursor starts moving.',
    },
    {
      name: 'travelDuration',
      required: false,
      type: 'number',
      description: 'Frames to travel from start to end on the house spring.',
    },
    {
      name: 'click',
      required: false,
      type: 'boolean',
      description: 'Emit a click ripple on arrival.',
    },
    {
      name: 'clickDelay',
      required: false,
      type: 'number',
      description: 'Frames after arrival before the click fires.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Pointer + ripple color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `text`).',
    },
    {
      name: 'size',
      required: false,
      type: 'number',
      description: 'Pointer height in px.',
    },
  ],
  DeviceFrame: [
    {
      name: 'device',
      required: false,
      type: "'phone' | 'laptop'",
      description: 'Which device bezel to draw.',
    },
    {
      name: 'src',
      required: false,
      type: 'string',
      description:
        'Image src shown inside when no `children` are passed (use the literal `"DEMO_IMAGE"` token in demos). Sized via the engine, not `object-fit`.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the entrance begins.',
    },
    {
      name: 'animate',
      required: false,
      type: 'boolean',
      description: 'Scale-and-fade the device in on the house spring.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Device width in px (height is derived from the device aspect).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Bezel color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `surface`).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: 'Content to wrap (scene nodes). Takes precedence over `src`.',
    },
  ],
  DrawOn: [
    {
      name: 'd',
      required: false,
      type: 'string',
      description:
        "SVG path `d` attribute (in the path's own coordinate space). The default is a gentle wave — on-brand.",
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Stroke color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `text`).',
    },
    {
      name: 'strokeWidth',
      required: false,
      type: 'number',
      description: 'Stroke width in path coordinate units.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the draw-on starts.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames to fully draw the path in (default `DURATION.slow` = 24).',
    },
  ],
  DynamicGrid: [
    {
      name: 'cell',
      required: false,
      type: 'number',
      description: 'Cell size in px (the lattice pitch).',
    },
    {
      name: 'variant',
      required: false,
      type: "'lines' | 'dots'",
      description: 'Ruled lines or a dot lattice.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Grid color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `border`).',
    },
    {
      name: 'speed',
      required: false,
      type: 'number',
      description: 'Diagonal drift speed in px/frame. Negative drifts the other way.',
    },
    {
      name: 'opacity',
      required: false,
      type: 'number',
      description: 'Grid opacity, 0..1 — a grid is scaffold, not subject.',
    },
    {
      name: 'glow',
      required: false,
      type: 'boolean',
      description: 'Add a centered accent glow over the grid.',
    },
    {
      name: 'glowColor',
      required: false,
      type: 'string',
      description:
        'Glow color (hex). The meaningful color on the CPU fallback (first stop) (default: theme `accent`).',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description: 'Canvas color painted behind the grid (default: theme `background`).',
    },
    {
      name: 'thickness',
      required: false,
      type: 'number',
      description: 'Stroke thickness (lines) / dot radius in px (dots).',
    },
  ],
  EndCard: [
    {
      name: 'cta',
      required: false,
      type: 'string',
      description: 'Hero CTA / headline line.',
    },
    {
      name: 'handles',
      required: false,
      type: 'string[]',
      description: 'Social handles or URLs displayed in a row beneath the CTA.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the CTA starts. The whole card is sequenced relative to this.',
    },
    {
      name: 'accent',
      required: false,
      type: 'boolean',
      description: 'Show the accent underline beneath the CTA (default `true`).',
    },
    {
      name: 'ctaFontSize',
      required: false,
      type: 'number',
      description: 'CTA font size in px (default 96).',
    },
    {
      name: 'ctaFontWeight',
      required: false,
      type: 'number',
      description: 'Font weight for the CTA (default 600).',
    },
    {
      name: 'handlesFontSize',
      required: false,
      type: 'number',
      description: 'Handles row font size in px (default 24).',
    },
    {
      name: 'handlesFontWeight',
      required: false,
      type: 'number',
      description: 'Font weight for the handles row (default 600).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'CTA color (default: theme `text`).',
    },
    {
      name: 'handlesColor',
      required: false,
      type: 'string',
      description: 'Handles color — defaults so the row reads quiet (default: theme `textMuted`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Underline color — the earned rose (default: theme `accent`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded display font for both CTA and handles (e.g. a `--font` passed to render) (default: theme `fontFamily`).',
    },
  ],
  FadeIn: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames to wait before starting.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames the fade takes to settle (default `DURATION.base` = 18).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: '',
    },
  ],
  FadeOut: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frame at which the exit begins.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames the fade-out takes (default `DURATION.fast` = 10 — exits are quick).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: '',
    },
  ],
  GradientShift: [
    {
      name: 'from',
      required: false,
      type: 'string',
      description:
        'Gradient start color (`#rrggbb` / `#rrggbbaa`). Default is the canvas tone — near-identical to `to`, so the drift reads as a dark-on-dark breath (default: theme `background`).',
    },
    {
      name: 'to',
      required: false,
      type: 'string',
      description:
        'Gradient end color. Default is one step warmer than `from`, intentionally near-identical so the shift is a whisper rather than a colored wash (default: theme `surface`).',
    },
    {
      name: 'angle',
      required: false,
      type: 'number',
      description:
        'Starting gradient angle in degrees (CSS convention: `0deg` points up, increasing clockwise). Default `135`.',
    },
    {
      name: 'speed',
      required: false,
      type: 'number',
      description:
        'Rotation rate in degrees per frame. Keep low — atmospheric, not focal. At 30fps the default `0.5` produces a 24-second full rotation.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description:
        'Frames before the drift starts. While `frame < delay` the gradient sits at `angle`. Default `0`.',
    },
  ],
  GrainOverlay: [
    {
      name: 'opacity',
      required: false,
      type: 'number',
      description:
        "Layer opacity, multiplied into every grain dot's alpha. Capped at `0.15` to match ondajs (CLAUDE.md tokens cap grain at ~2%; 0.02–0.04 is the house range). Default `0.04`.",
    },
    {
      name: 'baseFrequency',
      required: false,
      type: 'number',
      description:
        'Grain frequency. Higher = finer, tighter grain (more, smaller dots); lower = coarser photo-grain (fewer, larger dots). Mapped onto the dot count/size, not a true SVG turbulence frequency. Default `0.9`.',
    },
    {
      name: 'numOctaves',
      required: false,
      type: 'number',
      description:
        'Grain complexity. Higher values widen the per-dot alpha variance so the texture gains contrast/detail. Clamped to `1..4` like ondajs. Default `1`.',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description:
        'Deterministic variation — the same seed always produces the same grain. Default `0`.',
    },
    {
      name: 'count',
      required: false,
      type: 'number',
      description:
        'Number of grain dots to scatter. Kept modest by default to stay cheap; the effective count also scales mildly with `baseFrequency`. Default `800`.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description:
        "Grain color (hex `#rrggbb`). Matches ondajs's monochrome alpha grain. Use a light color over dark footage if desired (default: theme `background`).",
    },
    {
      name: 'animate',
      required: false,
      type: 'boolean',
      description:
        'When `true`, the scatter re-seeds on a frame bucket so the grain animates (a faint shimmer). Off by default — ondajs grain is intentionally static.',
    },
    {
      name: 'animateEvery',
      required: false,
      type: 'number',
      description: 'Frames per re-seed bucket when `animate` is on. Lower = busier. Default `2`.',
    },
  ],
  Highlight: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'Text to highlight.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the text starts revealing.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Text reveal duration in frames (default `DURATION.base` = 18).',
    },
    {
      name: 'lineDelay',
      required: false,
      type: 'number',
      description: 'Frames to wait after the text appears before the accent bar wipes in.',
    },
    {
      name: 'lineDuration',
      required: false,
      type: 'number',
      description:
        'Accent-bar wipe duration. Fast on purpose — emphatic (default `DURATION.fast`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Accent (highlight) bar color (default: theme `accent`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 64).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family.',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'paddingX',
      required: false,
      type: 'number',
      description: 'Pixels past the text edges that the accent bar extends (default 8).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Explicit text width in px. Overrides the glyph-count estimate when known.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Local-space placement of the component's top-left.",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Local-space placement of the component's top-left.",
    },
  ],
  IconPop: [
    {
      name: 'glyph',
      required: false,
      type: 'string',
      description:
        'A character/emoji to pop in (e.g. "✦", "★", "🎉"). Takes precedence over `shape` when set. Rendered via `<Text>` so it works on both backends.',
    },
    {
      name: 'shape',
      required: false,
      type: 'IconShape',
      description:
        'One of the four built-in shapes (check/cross/dot/star). Used when `glyph` is not set. Drawn as `<Path>` — GPU/Vello only.',
    },
    {
      name: 'iconSize',
      required: false,
      type: 'number',
      description: 'Icon size in px (the icon is square-ish, centered on its placement point).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Icon color (default: theme `accent`).',
    },
    {
      name: 'strokeWidth',
      required: false,
      type: 'number',
      description:
        'Stroke width for the outline shapes (check, cross). Ignored by glyph and by the filled shapes (dot, star).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the pop starts.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames the pop takes to settle (default `DURATION.base` = 18).',
    },
    {
      name: 'overshoot',
      required: false,
      type: 'number',
      description:
        'Overshoot amount — how far past 1.0 the scale peaks before settling, as a fraction (default 0.18 ≈ an 18% bump). 0 disables the overshoot.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Canvas x of the icon's CENTER (the pop grows from here).",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Canvas y of the icon's CENTER.",
    },
  ],
  ImageReveal: [
    {
      name: 'src',
      required: true,
      type: 'string',
      description: 'Image URL or path (resolved at render time).',
    },
    {
      name: 'motion',
      required: false,
      type: 'ImageRevealMotion',
      description: "Which motion fingerprint the entrance uses (default `'wipe'`).",
    },
    {
      name: 'fit',
      required: false,
      type: 'ImageRevealFit',
      description: "How the image fits its box (default `'cover'`).",
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames to fully reveal (default `DURATION.base` = 18).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the reveal starts.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Box top-left X in px (default 0).',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Box top-left Y in px (default 0).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Box width in px. Defaults to the full composition width (fill mode).',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Box height in px. Defaults to the full composition height (fill mode).',
    },
    {
      name: 'srcWidth',
      required: false,
      type: 'number',
      description:
        'Intrinsic source pixel width, used for `fit` math. Defaults to the box width (a plain stretch). Supply the real source size for true cover/contain.',
    },
    {
      name: 'srcHeight',
      required: false,
      type: 'number',
      description: 'Intrinsic source pixel height (see srcWidth).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description:
        'Corner radius of the box (clips the image to a rounded rect) (default: theme `radius`).',
    },
  ],
  InputField: [
    {
      name: 'value',
      required: false,
      type: 'string',
      description: "The field's value. With `typed` on, this is what types itself in.",
    },
    {
      name: 'placeholder',
      required: false,
      type: 'string',
      description: 'Placeholder shown while the field is empty (before any glyph is revealed).',
    },
    {
      name: 'label',
      required: false,
      type: 'string',
      description: 'Label above the field. Empty string hides it.',
    },
    {
      name: 'typed',
      required: false,
      type: 'boolean',
      description: 'Animate `value` typing itself in character-by-character (`useTextReveal`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before typing starts.',
    },
    {
      name: 'typeDuration',
      required: false,
      type: 'number',
      description: 'Frames to type the whole value (linear pacing).',
    },
    {
      name: 'focusRing',
      required: false,
      type: 'boolean',
      description: 'Show the accent focus ring around the field once typing begins.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Field width in px. Sized for a 1080p+ video canvas, not a screen UI.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Text size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'UI font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description: 'Value text color (default: theme `text`).',
    },
    {
      name: 'placeholderColor',
      required: false,
      type: 'string',
      description: 'Placeholder text color (default: theme `textMuted`).',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Label text color (default: theme `textMuted`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description:
        'Caret + focus-ring color — the one earned accent (the Onda rose) (default: theme `accent`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Resting (unfocused) field border color (default: theme `border`).',
    },
    {
      name: 'fieldColor',
      required: false,
      type: 'string',
      description: 'Field background fill (the "glass" surface) (default: theme `surface`).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Horizontal center of the field as a 0–1 fraction of canvas width.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Vertical center of the field as a 0–1 fraction of canvas height.',
    },
  ],
  KanbanBoard: [
    {
      name: 'columns',
      required: false,
      type: 'KanbanColumn[]',
      description: 'The columns, laid out left-to-right. Each holds its own ticket cards.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Overall board width in px. Split evenly across the columns.',
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Gap between columns (and between cards within a column) in px.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the first card enters.',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between successive cards rising in (house stagger = 4).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Base column-header font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family for headers and ticket labels (default: theme `fontFamily`).',
    },
    {
      name: 'accent',
      required: false,
      type: 'string',
      description:
        'Default accent for the dot/count when a column omits its own (default: theme `accent`).',
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description: 'Header / title text color (default: theme `text`).',
    },
    {
      name: 'cardTextColor',
      required: false,
      type: 'string',
      description: 'Ticket-label text color (default: theme `textMuted`).',
    },
    {
      name: 'faintColor',
      required: false,
      type: 'string',
      description:
        'Faint color for neutral dots, counts, and card accent stripes (default: theme `textMuted`).',
    },
    {
      name: 'columnFill',
      required: false,
      type: 'string',
      description:
        'Glass column fill (translucent — see approximations) (default: theme `surface`).',
    },
    {
      name: 'columnStroke',
      required: false,
      type: 'string',
      description: 'Glass column border color (default: theme `border`).',
    },
    {
      name: 'cardFill',
      required: false,
      type: 'string',
      description:
        'Ticket card fill (translucent — see approximations) (default: theme `surface`).',
    },
  ],
  KenBurns: [
    {
      name: 'src',
      required: true,
      type: 'string',
      description: 'Image source (resolved at render time by `onda render`).',
    },
    {
      name: 'imageWidth',
      required: false,
      type: 'number',
      description:
        'Natural pixel width of the source image. Only its ratio to `imageHeight` matters — it sets the cover fit. Default 1920.',
    },
    {
      name: 'imageHeight',
      required: false,
      type: 'number',
      description: 'Natural pixel height of the source image. Default 1080.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the drift starts.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames over which the zoom + pan completes. 150f ≈ 5s @ 30fps.',
    },
    {
      name: 'fromScale',
      required: false,
      type: 'number',
      description: 'Starting scale (atop the cover fit). Default 1.0.',
    },
    {
      name: 'toScale',
      required: false,
      type: 'number',
      description: 'Ending scale — keep the delta restrained (1.0 → 1.1). Default 1.1.',
    },
    {
      name: 'fromX',
      required: false,
      type: 'number',
      description: 'Starting pan origin X. `0` = left, `1` = right. Default 0.5 (center).',
    },
    {
      name: 'fromY',
      required: false,
      type: 'number',
      description: 'Starting pan origin Y. `0` = top, `1` = bottom. Default 0.5 (center).',
    },
    {
      name: 'toX',
      required: false,
      type: 'number',
      description: 'Ending pan origin X. Default 0.5.',
    },
    {
      name: 'toY',
      required: false,
      type: 'number',
      description: 'Ending pan origin Y. Default 0.5.',
    },
  ],
  LineChart: [
    {
      name: 'data',
      required: false,
      type: 'number[]',
      description: 'The series values, left to right.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the line starts drawing.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames for the line to fully draw on.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Line + dot color — the earned accent (default: theme `accent`).',
    },
    {
      name: 'strokeWidth',
      required: false,
      type: 'number',
      description: 'Stroke width in px.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Chart width in px.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Chart height in px.',
    },
    {
      name: 'fill',
      required: false,
      type: 'boolean',
      description: 'Fill a soft gradient area under the line.',
    },
    {
      name: 'showDots',
      required: false,
      type: 'boolean',
      description: 'Show a dot at each data point as the line reaches it.',
    },
  ],
  LogoSting: [
    {
      name: 'd',
      required: false,
      type: 'string',
      description: 'SVG path `d` for the logo mark, in `viewBox` coordinate space.',
    },
    {
      name: 'title',
      required: false,
      type: 'string',
      description: 'The brand / product title beneath the mark.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the sting starts.',
    },
    {
      name: 'accent',
      required: false,
      type: 'boolean',
      description: 'Draw the accent rule beneath the title (the single earned-color moment).',
    },
    {
      name: 'viewBox',
      required: false,
      type: 'string',
      description: 'SVG viewBox `"minX minY width height"` — must match the space of `d`.',
    },
    {
      name: 'pathWidth',
      required: false,
      type: 'number',
      description: 'Rendered width of the mark in px.',
    },
    {
      name: 'pathHeight',
      required: false,
      type: 'number',
      description: 'Rendered height of the mark in px.',
    },
    {
      name: 'strokeWidth',
      required: false,
      type: 'number',
      description: 'Stroke width, in px (after the viewBox→pixel scale; see note below).',
    },
    {
      name: 'stroke',
      required: false,
      type: 'string',
      description: 'Logo stroke color (default: theme `text`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Underline accent color — the signature dusty rose (default: theme `accent`).',
    },
    {
      name: 'titleFontSize',
      required: false,
      type: 'number',
      description: 'Title font size in px.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Title color (default: theme `text`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Display font family (must be loaded at render time) (default: theme `headingFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Title font weight (display default 600).',
    },
  ],
  LowerThird: [
    {
      name: 'name',
      required: false,
      type: 'string',
      description: "The person's name (the primary line).",
    },
    {
      name: 'role',
      required: false,
      type: 'string',
      description: "The person's role / title (the secondary line).",
    },
    {
      name: 'placement',
      required: false,
      type: 'LowerThirdPlacement',
      description:
        "Which canvas corner the bar sits in (default `'bottom-left'`). Drives the slide-in direction and flush alignment.",
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the name slides in.',
    },
    {
      name: 'accent',
      required: false,
      type: 'boolean',
      description: 'Show the accent rule beneath the name (default `true`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Name color (default: theme `text`).',
    },
    {
      name: 'roleColor',
      required: false,
      type: 'string',
      description: 'Role color (default: theme `textMuted`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Accent rule color (default: theme `accent`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Name font size in px (default 48).',
    },
    {
      name: 'nameFontWeight',
      required: false,
      type: 'number',
      description: 'Name font weight (default 600).',
    },
    {
      name: 'roleFontSize',
      required: false,
      type: 'number',
      description: 'Role font size in px (default 22).',
    },
    {
      name: 'roleFontWeight',
      required: false,
      type: 'number',
      description: 'Role font weight (default 500).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family for both lines (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description:
        'Accent rule corner radius in px (capped so a thin sliver never bulges) (default: theme `radius`).',
    },
  ],
  Marquee: [
    {
      name: 'items',
      required: false,
      type: 'string[]',
      description: 'Items to scroll. The list is repeated as needed for a seamless wrap.',
    },
    {
      name: 'speed',
      required: false,
      type: 'number',
      description: 'Scroll speed in pixels per second. Keep low for restraint (default 30).',
    },
    {
      name: 'direction',
      required: false,
      type: "'left' | 'right'",
      description: "Scroll direction (default `'left'`).",
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Pixels between items (default 64).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `textMuted`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 32).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'CSS weight 1..1000 (default 500).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Viewport width to scroll within. Defaults to the full composition width.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Viewport height (the clip band). Defaults to the full composition height.',
    },
  ],
  MaskReveal: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'What to reveal (single line). Ignored when `children` is supplied.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the reveal starts.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames for the mask to fully retreat.',
    },
    {
      name: 'direction',
      required: false,
      type: "'left' | 'right' | 'top' | 'bottom'",
      description:
        'The side the content appears to come IN from (mirrors `SlideIn`). The mask retreats toward this side.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Text size in px (default 96).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `headingFamily ?? fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic text.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description:
        'Clip-box width in px. Required for `children`; otherwise estimated from `text`.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Clip-box height in px. Required for `children`; otherwise `fontSize × 1.2`.',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: 'Arbitrary subtree to reveal instead of `text`. Supply `width`/`height`.',
    },
  ],
  MatrixDecode: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The text that decodes into place.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before decoding starts.',
    },
    {
      name: 'charDelay',
      required: false,
      type: 'number',
      description: 'Frames between successive characters settling (left-to-right).',
    },
    {
      name: 'scrambleDuration',
      required: false,
      type: 'number',
      description: 'Frames each character scrambles before it settles (min 1).',
    },
    {
      name: 'scrambleSpeed',
      required: false,
      type: 'number',
      description: 'Frames between glyph swaps while scrambling. Lower = faster flicker (min 1).',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description: 'Seed for the (deterministic) glyph picks.',
    },
    {
      name: 'charset',
      required: false,
      type: 'string',
      description: 'Glyph pool drawn from while scrambling.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Settled text color (default: theme `text`).',
    },
    {
      name: 'scrambleColor',
      required: false,
      type: 'string',
      description:
        'Color of still-scrambling glyphs — the earned accent (default: theme `accent`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 120).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Monospace stack keeps the advance steady as glyphs flicker (default: theme `monoFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (default 600).',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic text.',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: 'Horizontal anchoring of the single line (approximate — see file notes).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Absolute x of the line. Defaults to the canvas center (per `align`).',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Absolute y (top-ish) of the line. Defaults to vertical center.',
    },
  ],
  MeshGradient: [
    {
      name: 'colors',
      required: false,
      type: 'string[]',
      description:
        'Blob colors. 2–4 reads best; they drift over the `background` canvas (default: theme `palette[0]`).',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description: 'Base canvas color behind the blobs (default: theme `background`).',
    },
    {
      name: 'speed',
      required: false,
      type: 'number',
      description: 'Drift speed multiplier. Keep low — this is atmosphere, not motion.',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description: 'Seed for the blob phase/amplitude offsets (deterministic).',
    },
    {
      name: 'opacity',
      required: false,
      type: 'number',
      description: 'Overall blob opacity over the canvas (0..1).',
    },
  ],
  NodeGraph: [
    {
      name: 'hubLabel',
      required: false,
      type: 'string',
      description: 'Label inside the central hub node — a single character or short word.',
    },
    {
      name: 'satellites',
      required: false,
      type: 'Satellite[]',
      description:
        'The orbiting satellites. Each flies in from off-frame, then settles into its elliptical orbit. ~5 reads as a believable constellation.',
    },
    {
      name: 'accent',
      required: false,
      type: 'string',
      description:
        'The earned accent — hub fill tint, the connection lines, and the glow (default: theme `accent`).',
    },
    {
      name: 'ellipse',
      required: false,
      type: 'number',
      description: 'Vertical squash of every orbit (1 = circular, <1 = elliptical).',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description: 'Seed for the deterministic fly-in directions and connection-pulse phases.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the constellation begins assembling.',
    },
    {
      name: 'glow',
      required: false,
      type: 'boolean',
      description: 'Show the soft accent glow behind the hub.',
    },
    {
      name: 'hubDiameter',
      required: false,
      type: 'number',
      description: 'Hub node diameter in px.',
    },
    {
      name: 'hubFontSize',
      required: false,
      type: 'number',
      description: 'Hub label size in px.',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description:
        'Background canvas color behind the constellation (default: theme `background`).',
    },
    {
      name: 'surface',
      required: false,
      type: 'string',
      description: 'Surface (fill) color of the satellite pills (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Border color of the satellite pills (default: theme `border`).',
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description: 'Text color of every label (default: theme `text`).',
    },
    {
      name: 'satelliteFontSize',
      required: false,
      type: 'number',
      description: 'Satellite label font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Display font for every label (default: theme `fontFamily`).',
    },
    {
      name: 'centerX',
      required: false,
      type: 'number',
      description: 'Horizontal center of the constellation as a 0–1 fraction of canvas width.',
    },
    {
      name: 'centerY',
      required: false,
      type: 'number',
      description: 'Vertical center of the constellation as a 0–1 fraction of canvas height.',
    },
  ],
  Parallax: [
    {
      name: 'src',
      required: false,
      type: 'string',
      description: 'Single image layer. Use `src` OR `layers`; `layers` wins when both are set.',
    },
    {
      name: 'layers',
      required: false,
      type: 'ParallaxLayer[]',
      description: 'Multiple layers, drawn back-to-front, each drifting at its own `speed`.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the drift starts.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description:
        'Frames over which the drift completes (180f ≈ 6s @ 30fps — parallax wants time).',
    },
    {
      name: 'direction',
      required: false,
      type: "'left' | 'right' | 'up' | 'down'",
      description: 'The edge the layers drift *toward* as time advances.',
    },
    {
      name: 'distance',
      required: false,
      type: 'number',
      description:
        'Base drift in pixels across `duration` (per-layer scaled by `speed`). Keep restrained — past ~120px it reads as a pan, not parallax.',
    },
    {
      name: 'scale',
      required: false,
      type: 'number',
      description: "Default oversize applied to layers that don't set their own `scale` (1.05).",
    },
  ],
  PieReveal: [
    {
      name: 'data',
      required: false,
      type: 'PieRevealSlice[]',
      description: "Slices to render, drawn clockwise from 12 o'clock in array order.",
    },
    {
      name: 'radius',
      required: false,
      type: 'number',
      description: 'Outer radius of the pie, in px.',
    },
    {
      name: 'innerRadius',
      required: false,
      type: 'number',
      description:
        'Inner radius (donut hole) in px. `0` is a solid pie. The hole is filled with holeColor, so set that to match the background.',
    },
    {
      name: 'holeColor',
      required: false,
      type: 'string',
      description:
        'Color filling the donut hole — match the composition background (default: theme `background`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the **first** slice starts sweeping.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Per-slice sweep duration on the house spring.',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between consecutive slices starting (default canonical `STAGGER`).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Horizontal center as a 0–1 fraction of canvas width.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Vertical center as a 0–1 fraction of canvas height.',
    },
    {
      name: 'showLabel',
      required: false,
      type: 'boolean',
      description: 'Show the total/center label (donut only).',
    },
    {
      name: 'label',
      required: false,
      type: 'string',
      description: 'Center label text. Defaults to the slice count.',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Center label color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Center label font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Center label font family (must be loaded by the renderer) (default: theme `headingFamily ?? fontFamily`).',
    },
  ],
  PricingCard: [
    {
      name: 'tier',
      required: false,
      type: 'string',
      description: "Tier name above the price (e.g. `'Pro'`). Rendered uppercase.",
    },
    {
      name: 'price',
      required: false,
      type: 'string',
      description: "The headline price, rendered large. Free-form: `'$29'`, `'€19'`, `'Free'`.",
    },
    {
      name: 'period',
      required: false,
      type: 'string',
      description: "Billing period beneath the price (e.g. `'/month'`). Empty hides it.",
    },
    {
      name: 'features',
      required: false,
      type: 'string[]',
      description: 'Feature checklist — each item gets an accent checkmark, revealed on a stagger.',
    },
    {
      name: 'cta',
      required: false,
      type: 'string',
      description: 'Call-to-action button label.',
    },
    {
      name: 'recommended',
      required: false,
      type: 'boolean',
      description: 'Lifts + scales the card and shows an accent badge — the highlighted tier.',
    },
    {
      name: 'accent',
      required: false,
      type: 'string',
      description:
        'The earned accent — checkmarks, badge, CTA, recommended border + glow (default: theme `accent`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the card enters.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Card width in px.',
    },
    {
      name: 'priceSize',
      required: false,
      type: 'number',
      description: 'Price font size in px (the large display number).',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description: 'Panel fill color (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Panel border color (when not `recommended`) (default: theme `border`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Primary text color (price, features) (default: theme `text`).',
    },
    {
      name: 'dimColor',
      required: false,
      type: 'string',
      description: 'Dim color for the tier label (default: theme `textMuted`).',
    },
    {
      name: 'faintColor',
      required: false,
      type: 'string',
      description: 'Faint color for the billing period (default: theme `textMuted`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Display font for the price (default: theme `headingFamily ?? fontFamily`).',
    },
    {
      name: 'bodyFontFamily',
      required: false,
      type: 'string',
      description: 'Body font for tier / features / CTA (default: theme `fontFamily`).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Local-space x of the card's top-left. Omit to center on the composition.",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Local-space y of the card's top-left. Omit to center on the composition.",
    },
  ],
  ProgressBar: [
    {
      name: 'value',
      required: false,
      type: 'number',
      description: 'Target fill, 0–100. The bar grows from 0 to this value.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the animation starts.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames to reach the full target value (default `DURATION.slow` = 24).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Track width in px — the full 0%→100% travel.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Bar thickness in px.',
    },
    {
      name: 'radius',
      required: false,
      type: 'number',
      description: 'Corner radius in px. Defaults to a full pill.',
    },
    {
      name: 'trackColor',
      required: false,
      type: 'string',
      description: 'Track color — the unfilled portion (default: theme `surface`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Fill color (default: theme `accent`).',
    },
    {
      name: 'showValue',
      required: false,
      type: 'boolean',
      description: 'Whether to render the `${value}%` label beside the bar.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Label color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Label font family (must be loaded by the renderer).',
    },
  ],
  ProgressSteps: [
    {
      name: 'steps',
      required: false,
      type: 'string[]',
      description: 'Step labels, left to right.',
    },
    {
      name: 'current',
      required: false,
      type: 'number',
      description: 'How many steps are complete — the fill animates to this index (0-based count).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the fill animates.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames for the fill to travel to `current`.',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description:
        'Completed / active color — the earned accent (Onda rose) (default: theme `accent`).',
    },
    {
      name: 'dimColor',
      required: false,
      type: 'string',
      description: 'Pending color (dots + connector track) (default: theme `border`).',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Label color (default: theme `textMuted`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family for labels (default: theme `fontFamily`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Overall width in px (dots are spaced across this).',
    },
    {
      name: 'dotSize',
      required: false,
      type: 'number',
      description: 'Dot diameter in px.',
    },
    {
      name: 'trackThickness',
      required: false,
      type: 'number',
      description: 'Connector track thickness in px.',
    },
  ],
  PulsingIndicator: [
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Dot + ring color (default: theme `accent`).',
    },
    {
      name: 'size',
      required: false,
      type: 'number',
      description: 'Dot diameter in px.',
    },
    {
      name: 'label',
      required: false,
      type: 'string',
      description: 'Optional label to the right of the dot. Empty hides it.',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Label color (default: theme `textMuted`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Label font family (must be loaded by the renderer) (default: theme `fontFamily`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px.',
    },
    {
      name: 'period',
      required: false,
      type: 'number',
      description: 'Frames per pulse cycle.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Placement of the indicator's top-left (the dot's bounding box).",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: '',
    },
  ],
  QuoteCard: [
    {
      name: 'quote',
      required: false,
      type: 'string',
      description: 'The pull-quote body. Revealed word-by-word on a slower-than-canonical stagger.',
    },
    {
      name: 'author',
      required: false,
      type: 'string',
      description: 'Attribution name.',
    },
    {
      name: 'role',
      required: false,
      type: 'string',
      description: 'Attribution role / title.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the quote starts.',
    },
    {
      name: 'accent',
      required: false,
      type: 'boolean',
      description: 'Show the accent divider between quote and attribution.',
    },
    {
      name: 'quoteFontSize',
      required: false,
      type: 'number',
      description: 'Quote font size in px.',
    },
    {
      name: 'quoteFontWeight',
      required: false,
      type: 'number',
      description: 'Quote font weight (display default 600).',
    },
    {
      name: 'authorFontSize',
      required: false,
      type: 'number',
      description: 'Author / role font size in px.',
    },
    {
      name: 'authorFontWeight',
      required: false,
      type: 'number',
      description: 'Author / role font weight.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Quote color (default: theme `text`).',
    },
    {
      name: 'authorColor',
      required: false,
      type: 'string',
      description: 'Author / role color (default: theme `textMuted`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Divider color (default: theme `accent`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family for every line (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'quoteWidth',
      required: false,
      type: 'number',
      description:
        'Wrap width for the quote in px. Defaults to ~44% of the composition width (the ondajs `40vw` pull-quote feel), so long quotes wrap onto multiple lines.',
    },
  ],
  RgbGlitch: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The text to glitch.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the effect starts.',
    },
    {
      name: 'baseSplit',
      required: false,
      type: 'number',
      description: 'Constant baseline channel split in px (the always-on chromatic edge).',
    },
    {
      name: 'intensity',
      required: false,
      type: 'number',
      description: 'Peak extra split in px during a glitch burst.',
    },
    {
      name: 'glitchPeriod',
      required: false,
      type: 'number',
      description: 'Frames between glitch bursts.',
    },
    {
      name: 'glitchDuration',
      required: false,
      type: 'number',
      description: 'Frames a glitch burst lasts.',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description: 'Seed for the (deterministic) burst jitter.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Base (center) text color (default: theme `text`).',
    },
    {
      name: 'redColor',
      required: false,
      type: 'string',
      description: 'Red-channel copy color (default: theme `accent`).',
    },
    {
      name: 'cyanColor',
      required: false,
      type: 'string',
      description: 'Cyan-channel copy color (default: theme `palette[1]`).',
    },
    {
      name: 'channelOpacity',
      required: false,
      type: 'number',
      description:
        'Opacity of the coloured channel copies (the screen-blend approximation — lower keeps the center read as clean white). Default 0.85.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (default 600).',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic text.',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: "Line alignment relative to the placement point. Default `'center'`.",
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Absolute x of the alignment anchor. Defaults to canvas horizontal center.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Absolute y (top-ish) of the line. Defaults to vertical center.',
    },
  ],
  RotateIn: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames to wait before starting.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames to settle to 0° (default `DURATION.base` = 18).',
    },
    {
      name: 'fromDegrees',
      required: false,
      type: 'number',
      description: 'Starting angle in degrees (clockwise). Safe zone: `[-12, +12]`. Default -8.',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: '',
    },
  ],
  ScaleIn: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'from',
      required: false,
      type: 'number',
      description: 'Starting scale (default 0.9; below ~0.85 reads as dramatic zoom).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: '',
    },
  ],
  ShimmerSweep: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The single line of text to sweep light across.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the sweep starts.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Frames for one sweep pass (default `DURATION.slower` = 30).',
    },
    {
      name: 'loop',
      required: false,
      type: 'boolean',
      description: 'Loop the sweep instead of a single pass.',
    },
    {
      name: 'interval',
      required: false,
      type: 'number',
      description: 'Frames between sweeps when looping.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description:
        'Base (dim) text color so the bright band reads as a highlight (default: theme `textMuted`).',
    },
    {
      name: 'shimmerColor',
      required: false,
      type: 'string',
      description: 'The sweeping highlight color (default: theme `text`).',
    },
    {
      name: 'angle',
      required: false,
      type: 'number',
      description: 'Sweep angle in degrees (approximated by tilting the gradient band).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 96).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Explicit text-box width in px. Overrides the glyph-count estimate.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Local-space placement of the component's top-left.",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Local-space placement of the component's top-left.",
    },
  ],
  SkeletonCard: [
    {
      name: 'lines',
      required: false,
      type: 'number',
      description: 'Number of placeholder text bars below the (optional) thumbnail.',
    },
    {
      name: 'thumbnail',
      required: false,
      type: 'boolean',
      description: 'Show the leading thumbnail block above the bars.',
    },
    {
      name: 'shimmerSpeed',
      required: false,
      type: 'number',
      description: 'Frames for one shimmer pass across the card. Lower = faster sweep.',
    },
    {
      name: 'shimmerColor',
      required: false,
      type: 'string',
      description:
        'The travelling highlight color — a soft sheen over the bars (default: theme `border`).',
    },
    {
      name: 'barColor',
      required: false,
      type: 'string',
      description: 'Resting fill of the placeholder bars / thumbnail (default: theme `surface`).',
    },
    {
      name: 'cardColor',
      required: false,
      type: 'string',
      description:
        'Card (panel) background — the translucent glass fill (default: theme `background`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Card border color (the 1px-equivalent stroke) (default: theme `border`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the card enters.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Card width in px.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Card height in px. `undefined` sizes the card to its content.',
    },
    {
      name: 'barHeight',
      required: false,
      type: 'number',
      description: 'Base bar height in px.',
    },
    {
      name: 'padding',
      required: false,
      type: 'number',
      description: 'Inner padding of the card in px.',
    },
  ],
  SlideIn: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'direction',
      required: false,
      type: "'up' | 'down' | 'left' | 'right'",
      description: "Settling direction (default `'up'`).",
    },
    {
      name: 'distance',
      required: false,
      type: 'number',
      description: 'Travel distance in px (12–24 Onda envelope; default 12).',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: '',
    },
  ],
  SlideOut: [
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'direction',
      required: false,
      type: "'up' | 'down' | 'left' | 'right'",
      description: "Direction the element leaves toward (default `'down'`).",
    },
    {
      name: 'distance',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: '',
    },
  ],
  SlotMachineRoll: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The text that rolls into place. Best on short strings (years, counts).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before rolling starts.',
    },
    {
      name: 'charDelay',
      required: false,
      type: 'number',
      description: 'Frames between successive characters starting their roll.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: "Frames for each character's reel to settle (default `DURATION.slow` = 24).",
    },
    {
      name: 'reelLength',
      required: false,
      type: 'number',
      description: 'How many filler glyphs spin past before the target lands.',
    },
    {
      name: 'seed',
      required: false,
      type: 'number',
      description: 'Seed for the (deterministic) filler glyphs.',
    },
    {
      name: 'charset',
      required: false,
      type: 'string',
      description: 'Glyph pool the reel spins through.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 140). The cell height equals this.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Monospace/display stack keeps reels column-aligned (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (default 600).',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic glyphs.',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: "Horizontal anchoring of the whole block (default `'center'`).",
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description:
        "Absolute x of the block's anchor. Defaults to the canvas horizontal center (respecting `align`).",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Absolute y of the block's top. Defaults to vertically centering the row.",
    },
  ],
  SplitScreen: [
    {
      name: 'left',
      required: false,
      type: 'ReactNode',
      description: 'Content for the left (or top) pane — any scene subtree.',
    },
    {
      name: 'right',
      required: false,
      type: 'ReactNode',
      description: 'Content for the right (or bottom) pane — any scene subtree.',
    },
    {
      name: 'orientation',
      required: false,
      type: "'horizontal' | 'vertical'",
      description: 'Pane axis: `horizontal` = side-by-side, `vertical` = stacked.',
    },
    {
      name: 'ratio',
      required: false,
      type: 'number',
      description: 'Fraction (0..1) of the main axis given to the `left` (or top) pane.',
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Gap between the two panes in px.',
    },
    {
      name: 'divider',
      required: false,
      type: 'boolean',
      description: 'Draw a thin token divider in the gap between the panes.',
    },
    {
      name: 'animate',
      required: false,
      type: 'boolean',
      description: 'Slide the two panes apart from the center seam on the house spring.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the entrance.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Overall width in px. Defaults to the full composition width.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Overall height in px. Defaults to the full composition height.',
    },
    {
      name: 'paneBackground',
      required: false,
      type: 'string',
      description: 'Pane background fill (default: theme `surface`).',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description:
        'Outer (gutter) background fill, seen in the gap behind the divider (default: theme `background`).',
    },
    {
      name: 'dividerColor',
      required: false,
      type: 'string',
      description: 'Divider color (thin token line) (default: theme `border`).',
    },
    {
      name: 'placeholderColor',
      required: false,
      type: 'string',
      description: 'Placeholder label color for an empty pane (default: theme `textMuted`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family for empty-pane placeholders (default: theme `fontFamily`).',
    },
  ],
  Spotlight: [
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Horizontal center of the spotlight as a 0–1 fraction of canvas width.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Vertical center of the spotlight as a 0–1 fraction of canvas height.',
    },
    {
      name: 'radius',
      required: false,
      type: 'number',
      description: "Final radius as a percentage of the canvas's smaller dimension.",
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the reveal starts.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames until the spotlight reaches its full radius.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Light color (default: theme `text`). Hex `#rrggbb` / `#rrggbbaa`.',
    },
    {
      name: 'softness',
      required: false,
      type: 'number',
      description:
        'Gradient softness — % of the radius given over to the fade-to-transparent tail. `0` is a hard disc; `100` fades from the very center.',
    },
  ],
  SpotlightCard: [
    {
      name: 'eyebrow',
      required: false,
      type: 'string',
      description: 'Small uppercase kicker above the title. Empty hides it.',
    },
    {
      name: 'title',
      required: false,
      type: 'string',
      description: 'Card headline (display font).',
    },
    {
      name: 'body',
      required: false,
      type: 'string',
      description:
        'Supporting body copy. Empty hides it. Single line (engine `<Text>` is single-line; pass a short string).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the card enters.',
    },
    {
      name: 'glowColor',
      required: false,
      type: 'string',
      description: 'The drifting spotlight color — the earned accent (default: theme `accent`).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Card width in px.',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Card height in px. If omitted, sized from the content + padding.',
    },
    {
      name: 'padding',
      required: false,
      type: 'number',
      description: 'Inner padding in px.',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center'",
      description: 'Text alignment within the card.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Display font for the title (default: theme `headingFamily ?? theme.fontFamily`).',
    },
    {
      name: 'bodyFontFamily',
      required: false,
      type: 'string',
      description: 'Font family for the eyebrow + body copy (default: theme `fontFamily`).',
    },
    {
      name: 'titleSize',
      required: false,
      type: 'number',
      description: 'Title font size in px (default 44).',
    },
    {
      name: 'bodySize',
      required: false,
      type: 'number',
      description: 'Body font size in px (default 20).',
    },
    {
      name: 'eyebrowSize',
      required: false,
      type: 'number',
      description: 'Eyebrow font size in px (default 15).',
    },
    {
      name: 'titleColor',
      required: false,
      type: 'string',
      description: 'Title color (default: theme `text`).',
    },
    {
      name: 'bodyColor',
      required: false,
      type: 'string',
      description: 'Body color (default: theme `textMuted`).',
    },
    {
      name: 'eyebrowColor',
      required: false,
      type: 'string',
      description: 'Eyebrow color (default: theme `textMuted`).',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description: 'Card glass fill (translucent dark by default) (default: theme `surface`).',
    },
    {
      name: 'borderColor',
      required: false,
      type: 'string',
      description: 'Card border (stroke) color (default: theme `border`).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description: 'Corner radius in px (default: theme `radius`).',
    },
  ],
  StaggerGroup: [
    {
      name: 'items',
      required: false,
      type: 'string[]',
      description: 'The items to reveal, in source order (default: four short lines).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the FIRST item starts (default 0).',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between consecutive items. Canonical Onda stagger is `4`.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Per-item reveal duration (default `DURATION.base` = 18).',
    },
    {
      name: 'direction',
      required: false,
      type: "'row' | 'column'",
      description: "Layout direction for the items (default `'column'`).",
    },
    {
      name: 'gap',
      required: false,
      type: 'number',
      description: 'Pixels between items (default 16).',
    },
    {
      name: 'align',
      required: false,
      type: "'start' | 'center' | 'end'",
      description: "Cross-axis alignment of items (default `'center'`).",
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 48).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (bundled: "Open Sans", "IBM Plex Sans") (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
  ],
  StatCard: [
    {
      name: 'value',
      required: true,
      type: 'string',
      description: 'The headline metric, e.g. "26.8 fps" or "100×".',
    },
    {
      name: 'label',
      required: true,
      type: 'string',
      description: 'The label beneath, e.g. "faster than Remotion".',
    },
    {
      name: 'valueSize',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'labelSize',
      required: false,
      type: 'number',
      description: '',
    },
    {
      name: 'valueColor',
      required: false,
      type: 'string',
      description: 'Value color (default: theme `text`).',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Label color (default: theme `textMuted`).',
    },
    {
      name: 'accent',
      required: false,
      type: 'string',
      description: 'Accent color for the underline bar (default: theme `accent`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family (default: theme body family).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: '',
    },
  ],
  Terminal: [
    {
      name: 'command',
      required: false,
      type: 'string',
      description: 'The command that types itself out after the prompt.',
    },
    {
      name: 'output',
      required: false,
      type: 'string[]',
      description: 'Output lines that appear, staggered, once the command finishes typing.',
    },
    {
      name: 'prompt',
      required: false,
      type: 'string',
      description: 'The shell prompt glyph.',
    },
    {
      name: 'title',
      required: false,
      type: 'string',
      description: 'Title-bar label. Empty hides it (dots still show if `chrome` is on).',
    },
    {
      name: 'chrome',
      required: false,
      type: 'boolean',
      description: 'Show window chrome (dots + title bar).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before typing starts.',
    },
    {
      name: 'typeSpeed',
      required: false,
      type: 'number',
      description: 'Frames to type the whole command (linear cadence).',
    },
    {
      name: 'outputDelay',
      required: false,
      type: 'number',
      description: 'Frames after the command finishes before output begins.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Monospace font stack (default: theme `monoFamily`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px. Sized for a 1080p+ video canvas, not a screen UI.',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description:
        "Width of the window in px. Fixed so the frame is stable while the command types into it — a terminal has a defined size, it doesn't grow char by char.",
    },
    {
      name: 'textColor',
      required: false,
      type: 'string',
      description: 'Command text color (default: theme `text`).',
    },
    {
      name: 'promptColor',
      required: false,
      type: 'string',
      description: 'Prompt glyph color — the earned accent (default: theme `accent`).',
    },
    {
      name: 'outputColor',
      required: false,
      type: 'string',
      description: 'Output line color (default: theme `textMuted`).',
    },
    {
      name: 'background',
      required: false,
      type: 'string',
      description: 'Window background color (default: theme `background`).',
    },
    {
      name: 'cornerRadius',
      required: false,
      type: 'number',
      description: 'Window corner radius in px (default: theme `radius`).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Absolute x of the window's top-left. Defaults to horizontally centered.",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Absolute y of the window's top-left. Defaults to vertically centered.",
    },
  ],
  TextFadeReplace: [
    {
      name: 'from',
      required: true,
      type: 'string',
      description: 'The outgoing phrase (shown first, fades out).',
    },
    {
      name: 'to',
      required: true,
      type: 'string',
      description: 'The incoming phrase (fades in over `from`).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description:
        'Frames before the crossfade begins. Until then only `from` is shown (default `DURATION.hold` = 45, a settled beat before the swap).',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description:
        'Frames the crossfade takes — old out over the first half, new in over the second (default `DURATION.base` = 18).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default `96`, matching the ondajs display default).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `text`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `headingFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (default `600`).',
    },
  ],
  Timeline: [
    {
      name: 'events',
      required: false,
      type: 'TimelineEvent[]',
      description: 'Anchor points down the timeline. Order is preserved — top to bottom.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the line begins to draw.',
    },
    {
      name: 'lineDuration',
      required: false,
      type: 'number',
      description: 'Frames over which the vertical line reveals itself top→bottom.',
    },
    {
      name: 'dotDelay',
      required: false,
      type: 'number',
      description: 'Frames between the line completing and the first dot appearing.',
    },
    {
      name: 'dotStagger',
      required: false,
      type: 'number',
      description: 'Frames between consecutive dot entrances (canonical Onda stagger = 4).',
    },
    {
      name: 'dotDuration',
      required: false,
      type: 'number',
      description: 'Per-dot entrance duration.',
    },
    {
      name: 'dotSize',
      required: false,
      type: 'number',
      description: 'Dot diameter in px.',
    },
    {
      name: 'spacing',
      required: false,
      type: 'number',
      description: 'Vertical distance between consecutive events, in px.',
    },
    {
      name: 'lineWidth',
      required: false,
      type: 'number',
      description: 'Line thickness in px.',
    },
    {
      name: 'lineColor',
      required: false,
      type: 'string',
      description: 'Line color (default: theme `border`).',
    },
    {
      name: 'dotColor',
      required: false,
      type: 'string',
      description: 'Non-final dot color (default: theme `text`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Final dot color — the earned accent (Onda rose) (default: theme `accent`).',
    },
    {
      name: 'labelColor',
      required: false,
      type: 'string',
      description: 'Label color (default: theme `textMuted`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Label font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family for labels (default: theme `headingFamily ?? fontFamily`).',
    },
  ],
  TitleCard: [
    {
      name: 'title',
      required: true,
      type: 'string',
      description: '',
    },
    {
      name: 'subtitle',
      required: false,
      type: 'string',
      description: '',
    },
    {
      name: 'titleSize',
      required: false,
      type: 'number',
      description: 'Title font size in px (default 96).',
    },
    {
      name: 'subtitleSize',
      required: false,
      type: 'number',
      description: 'Subtitle font size in px (default 36).',
    },
    {
      name: 'titleColor',
      required: false,
      type: 'string',
      description: 'Title color (default: theme `text`).',
    },
    {
      name: 'subtitleColor',
      required: false,
      type: 'string',
      description: 'Subtitle color (default: theme `textMuted`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family (default: theme heading family, else body family).',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frame the title begins fading in (subtitle follows by one stagger step).',
    },
  ],
  TrackingIn: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The text to settle in.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the entrance starts.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description: 'Frames until the text settles (default `DURATION.slow` = 24).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (hex `#rrggbb` / `#rrggbbaa`) (default: theme `text`).',
    },
    {
      name: 'fromTracking',
      required: false,
      type: 'number',
      description: 'Starting letter-spacing in em — the text begins spread wide and contracts.',
    },
    {
      name: 'tracking',
      required: false,
      type: 'number',
      description: 'Resting letter-spacing in em.',
    },
    {
      name: 'blur',
      required: false,
      type: 'boolean',
      description: 'Start the text soft and sharpen as it settles (approximated; see doc).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px.',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic text.',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: "Horizontal alignment of the line about `x`. Default `'center'`.",
    },
    {
      name: 'advanceFactor',
      required: false,
      type: 'number',
      description:
        'Estimated per-glyph advance as a fraction of `fontSize` (default 0.55 — a typical display-sans average; tune per font).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Absolute x anchor of the line (default canvas center).',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: "Absolute y of the line's top (default vertical center).",
    },
  ],
  Typewriter: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'What to type out.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before typing starts.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description:
        'Frames to type the full string. Linear pacing — chars-per-frame is constant. Default `DURATION.slow` (24).',
    },
    {
      name: 'cursor',
      required: false,
      type: 'boolean',
      description: 'Show a blinking cursor at the leading edge while typing. Default `true`.',
    },
    {
      name: 'cursorColor',
      required: false,
      type: 'string',
      description: 'Cursor color (default: theme `accent`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 64).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (default 500 — reads more "terminal").',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic text.',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: "Absolute x of the text's left edge. Defaults to ~8% of canvas width.",
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Absolute y baseline-ish top of the text. Defaults to vertical center.',
    },
  ],
  Underline: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'Text to reveal. Pass `""` to draw the rule alone.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the text starts revealing.',
    },
    {
      name: 'duration',
      required: false,
      type: 'number',
      description: 'Text reveal duration in frames (default `DURATION.base` = 18).',
    },
    {
      name: 'lineDelay',
      required: false,
      type: 'number',
      description: 'Frames to wait after the text lands before the rule starts drawing.',
    },
    {
      name: 'lineDuration',
      required: false,
      type: 'number',
      description: 'Rule draw duration. Fast on purpose — emphatic (default `DURATION.fast`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'accentColor',
      required: false,
      type: 'string',
      description: 'Rule color (default: theme `accent`).',
    },
    {
      name: 'lineThickness',
      required: false,
      type: 'number',
      description: 'Rule thickness in px.',
    },
    {
      name: 'lineOffset',
      required: false,
      type: 'number',
      description: 'Pixel gap between the text box and the rule.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Text size in px (default 64).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description: 'Loaded font family (e.g. a `--font` passed to `onda render`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description: 'Horizontal alignment of the rule under the text.',
    },
  ],
  VideoClip: [
    {
      name: 'src',
      required: true,
      type: 'string',
      description:
        'URL or path to the video. The current frame is decoded per composition frame by the player (browser: <video>/WebCodecs) or `onda export` (native ffmpeg).',
    },
    {
      name: 'startAt',
      required: false,
      type: 'number',
      description:
        'Seconds into the source shown at the clip’s frame 0 (trim the head). Default 0.',
    },
    {
      name: 'playbackRate',
      required: false,
      type: 'number',
      description:
        'Source seconds advanced per composition second (1 = realtime, 2 = 2×, 0.5 = slow-mo). Default 1.',
    },
    {
      name: 'previewFallback',
      required: false,
      type: "'skip' | 'element'",
      description:
        'Preview-only: how to preview a source the browser can’t composite (cross-origin without CORS). `skip` (default) blanks it + warns; `element` overlays a plain <video> so it still plays. Never affects `onda export` (ffmpeg composites any direct URL). For your own assets, serving them with CORS gives full compositing instead.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames the clip waits before its fade-in begins (default 0).',
    },
    {
      name: 'fadeIn',
      required: false,
      type: 'number',
      description: 'Frames the fade-in takes (default `DURATION.base` = 18). `0` = hard cut in.',
    },
    {
      name: 'fadeOut',
      required: false,
      type: 'number',
      description: 'Frames the fade-out takes (default `DURATION.base` = 18). `0` = hard cut out.',
    },
    {
      name: 'durationInFrames',
      required: false,
      type: 'number',
      description:
        'Visible hold of the clip in frames, used to time the fade-out (the fade-out lands on the last `fadeOut` frames). When omitted, the clip never fades out — mirrors ondajs skipping the fade-out when `endAt` is unset.',
    },
    {
      name: 'fit',
      required: false,
      type: "'cover' | 'contain'",
      description:
        "How the poster fits its box. `'cover'` crops to fill (default); `'contain'` letterboxes against black.",
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Box width in px the clip occupies (default = full composition width).',
    },
    {
      name: 'height',
      required: false,
      type: 'number',
      description: 'Box height in px the clip occupies (default = full composition height).',
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Top-left x of the box in px (default 0 — canvas-filling).',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Top-left y of the box in px (default 0 — canvas-filling).',
    },
    {
      name: 'sourceWidth',
      required: false,
      type: 'number',
      description:
        'Intrinsic poster width in px, used to compute the fit scale. Defaults to the composition width (a poster rendered at comp resolution scales 1:1).',
    },
    {
      name: 'sourceHeight',
      required: false,
      type: 'number',
      description: 'Intrinsic poster height in px. Defaults to the composition height.',
    },
    {
      name: 'borderRadius',
      required: false,
      type: 'number',
      description: 'Rounded corner radius of the box in px (default: theme `radius`).',
    },
    {
      name: 'letterbox',
      required: false,
      type: 'number',
      description:
        'Draw cinematic black letterbox bars top & bottom, each this many px tall (default 0 = none). Drawn over the poster, inside the box.',
    },
    {
      name: 'backgroundColor',
      required: false,
      type: 'string',
      description: 'Backing color shown behind/around the poster (default: theme `background`).',
    },
  ],
  Vignette: [
    {
      name: 'intensity',
      required: false,
      type: 'number',
      description: 'Edge darkness. `0` = no vignette, `1` = fully dark edges. Default `0.5`.',
    },
    {
      name: 'innerRadius',
      required: false,
      type: 'number',
      description:
        'Percent (0..100) from center where the darkening begins. Larger = bigger clean middle. Default `40`.',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description:
        'Edge color. Defaults to pure black for the classic cinematic frame (default: theme `background`).',
    },
  ],
  WordRotate: [
    {
      name: 'phrases',
      required: false,
      type: 'string[]',
      description: 'Phrases cycled in place, in order. One is visible at a time.',
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the first phrase begins to enter (default `0`).',
    },
    {
      name: 'holdDuration',
      required: false,
      type: 'number',
      description:
        'Frames each phrase holds at full opacity before the next arrives (default `30`).',
    },
    {
      name: 'transitionDuration',
      required: false,
      type: 'number',
      description:
        'Frames for a single phrase to fade in (and, separately, fade out) (default `12`).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px. Phrases are usually large (default `96`).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default `600`).',
    },
    {
      name: 'italic',
      required: false,
      type: 'boolean',
      description: 'Italic text (default `false`).',
    },
    {
      name: 'align',
      required: false,
      type: "'left' | 'center' | 'right'",
      description:
        "Horizontal anchor of each phrase relative to `x` (default `'left'`). `'center'`/`'right'` use an estimated text width (see doc comment).",
    },
    {
      name: 'x',
      required: false,
      type: 'number',
      description: 'Absolute x of the anchor point. Defaults to the composition center.',
    },
    {
      name: 'y',
      required: false,
      type: 'number',
      description: 'Absolute y (top-ish) of the text. Defaults to vertical center.',
    },
  ],
  WordStagger: [
    {
      name: 'text',
      required: false,
      type: 'string',
      description: 'The phrase. Split on whitespace into one reveal per word.',
    },
    {
      name: 'fontSize',
      required: false,
      type: 'number',
      description: 'Font size in px (default 64).',
    },
    {
      name: 'color',
      required: false,
      type: 'string',
      description: 'Text color (default: theme `text`).',
    },
    {
      name: 'width',
      required: false,
      type: 'number',
      description: 'Container width in px — the line wraps within this (default 1080).',
    },
    {
      name: 'fontFamily',
      required: false,
      type: 'string',
      description:
        'Loaded font family (e.g. a `--font` passed to `onda render`) (default: theme `fontFamily`).',
    },
    {
      name: 'fontWeight',
      required: false,
      type: 'number',
      description: 'Font weight (display default 600).',
    },
    {
      name: 'justify',
      required: false,
      type: "'start' | 'center' | 'end'",
      description: "Horizontal alignment of words within each line (default `'start'`).",
    },
    {
      name: 'delay',
      required: false,
      type: 'number',
      description: 'Frames before the FIRST word starts (default 0).',
    },
    {
      name: 'stagger',
      required: false,
      type: 'number',
      description: 'Frames between consecutive words (default `STAGGER` = 4).',
    },
  ],
}
